# use start to bring up the poc
start:
	docker-compose up -d -V

# use status to get the status of the poc
status:
	docker-compose status

# use stop to tear down the poc
stop:
	docker-compose down --rm
