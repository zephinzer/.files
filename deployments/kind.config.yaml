# start a kubernetes cluster by running:
#   `kind create cluster --name zdotfiles --config ./kind.config.yaml`
# from this directory

kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
  - role: worker
